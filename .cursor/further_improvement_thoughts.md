## Protect API
- Could have bad actors try and flood our server. 
	- rate limit accordingly.
- Should implement secret key auth, very basic


## Should host a frontend, but should have gemini build the app? Studio seems sweet, it stubbed out the API calls. Could get a nice design going.. figure out how to plug it in into railway 
## 