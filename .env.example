# QuantAgentic API Environment Variables
# Copy this file to .env and fill in your actual values
# The .env file is gitignored and will not be committed

# Redis Configuration (optional - defaults work for local development)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
# REDIS_PASSWORD=  # Optional, leave unset if not using password

# CrewAI / Gemini Configuration (required for disaster polling agent)
GEMINI_MODEL=gemini/gemini-3-pro-preview
GEMINI_API_KEY=your_gemini_api_key_here

# Executor Configuration (optional)
EXECUTOR_MAX_RETRIES=5

# NWS API Configuration (optional - defaults provided)
# NWS_USER_AGENT_NAME=quantagent_capital
# NWS_USER_AGENT_EMAIL=jacob@quantagent_capital.ai
